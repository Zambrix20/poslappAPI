swagger: "2.0"
info:
  version: "0.0.1"
  title: Hello World App
# during dev, should point to your local machine
host: localhost:10010
# basePath prefixes all resource paths 
basePath: /
# 
schemes:
  # tip: remove http to make production-grade
  - http
  - https
# format of bodies a client can send (Content-Type)
consumes:
  - application/json
# format of the responses to the client (Accepts)
produces:
  - application/json
paths:
  /usuarios:
    x-swagger-router-controller: usuarios.controller
    get:
      description: Mostrar los usuarios en lista (get the game system list)
      operationId: getUsuarios
      #parameters:
      #  - name: name
      #    in: query
      #    type: string
      #    required: false
      #  - name: sort
      #    in: query
      #   type: string
      #    required: false
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/GetUsuariosListResponse"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      description: Agrega un nuevo usuario a la lista (add a new game system to the list)
      operationId: createUsuarios
      parameters:
        - in: body
          name: Usuarios (gamesystem)
          description: El usuario a crear (The Game System to create.)
          schema:
            $ref: "#/definitions/Usuarios"
      responses:
        "201":
          description: Success
          schema:
            $ref: "#/definitions/GetUsuariosResponse"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  
  /usuarios/{id}:
    x-swagger-router-controller: usuarios.controller
    get:
      description: Dame un usuario por id (get a game system)
      operationId: getUsuariosByPk
      parameters:
        - name: id
          type: integer
          in: path
          required: true
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/GetUsuariosResponse"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    put:
      description: Actualizar los usuarios por id (update a game system)
      operationId: updateUsuarios
      # define the parameters
      parameters:
        - name: id
          description: Usuario Id (game system id)
          type: integer
          in: path
          required: true
        - in: body
          name: Usuario (gamesystem)
          description: El usuario a actualizar (The Game System to update.)
          schema:
            $ref: "#/definitions/Usuarios"
=======
  /zonas:
    x-swagger-router-controller: zonas.controller
    get:
      description: das
      operationId: getZonas

      responses:
        "200":
          description: Success
          schema:

            $ref: "#/definitions/GetZonasListResponse"
        # responses may fall through to errors

        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      description: add a new zona to the list
      operationId: addZonas
      parameters:
        - in: body
          name: nombreZonas
          description: The Zonas to create.
          schema:
            $ref: "#/definitions/Zona"
      responses:
        "201":
          description: Success
          schema:
            $ref: "#/definitions/GetZonasResponse"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /zonasById/{id}:
    x-swagger-router-controller: zonas.controller
    get:
      description: get a zona
      operationId: getZonasById
      parameters:
        - name: id
          type: number
          in: path
          required: true
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/GetZonasResponse"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /swagger:
    x-swagger-pipe: swagger_raw
# complex objects have schema definitions
definitions:
  Usuarios:
    type: object
    properties:
      #id_usuario:
      #  type: integer
      #  description: Id del usuario (Id of the Game System)
      Nombre_Usuario:
        type: string
        description: Nombre del usuario (Name of the Game System)
      Apellido:
        type: string
        description: Apellido del usuario (Description of the zone)
      #image:
      #  type: string
      #  format: binary
      Email:
        type: string
        description: Email del usuario
      Password:
        type: string
        description: Password del usuario
      Fecha_De_Nacimiento:
        type: string
        format: date
        description: Fecha de Nacimiento del usuario
      Telefono:
        type: string
        description: Telefono del usuario
    required:
      #- id_usuario
      - Nombre_Usuario
      - Apellido
      - Email
      - Password
      - Fecha_De_Nacimiento
      - Telefono
      
  GetUsuariosResponse:
    type: object
    properties:
      id_usuario:
        type: integer
        description: Id del usuario (Id of the Game System)
      Nombre_Usuario:
        type: string
        description: Nombre del usuario (Name of the Game System)
      Apellido:
        type: string
        description: Apellido del usuario (Description of the zone)
      Image:
        type: string
        format: binary
      Email:
        type: string
        description: Email del usuario
      Password:
        type: string
        description: Password del usuario
      Fecha_De_Nacimiento:
        type: integer
        description: Fecha de Nacimiento del usuario
      Telefono:
        type: string
        description: Telefono del usuario
  GetUsuariosListResponse:
    required:
      - gamesystems
    properties:
      gamesystems:
        type: array
        items:
          $ref: "#/definitions/GetUsuariosResponse"
  #GeneralResponse:
  #  type: object
  #  properties:
  #    success:
  #      type: integer
  #      description: returns 1 if successful
  #    description:
  #      type: string
  #      description: a short comment
  #  required:
  #    - success
  #    - description
=======
  Zona:
    type: object
    properties:
      nombreZonas:
        type: string
        description: Name of the Zona
    required:
      - nombreZonas
  GetZonasResponse:
    type: object
    properties:
      id:
        type: number
        description: identifier
      name:
        type: string
        description: Nombre de la zona
    required:
      - name
  GetZonasListResponse:
    required:
      - zonas
    properties:
      zonas:
        type: array
        items:
          $ref: "#/definitions/GetZonasResponse"
  ErrorResponse:
    required:
      - message
    properties:
      code:
        type: number
      message:
        type: string
      description:
        type: string